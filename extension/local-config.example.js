// Optional development-only defaults. Copy to local-config.js, which is ignored by Git.
export const DEFAULT_PORT = 32145
export const DEFAULT_TOKEN = 'replace-with-a-random-token-of-at-least-16-characters'
